# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

GET     /                               controllers.HomeController.index
GET     /books                          controllers.BookController.getBooks()
GET     /books/test                         controllers.BookController.getCat()
GET     /books/search/:searchInput             controllers.BookController.searchBooks(searchInput:String)
GET     /books/title/:title             controllers.BookController.getBooksByTitle(title:String)
GET     /books/featured                 controllers.BookController.getFeaturedBooks(size:Integer  ?= 4)
GET     /books/:id                      controllers.BookController.getBooksById(id:Long)
GET     /books/category/:category       controllers.BookController.getBooksByCategory(category:String)
GET     /books/writer/:firstName/:lastName                  controllers.BookController.getBooksByWriter(firstName:String,lastName:String)
# Date format in path 2019-08-15
GET     /books/publishingDate/:publishingDate               controllers.BookController.getBooksByPublishingDate(publishingDate:String)
GET     /books/publishingDate/:startDate/:endDate           controllers.BookController.getBooksByPublishingPeriod(startDate:String, endDate:String, size: Integer ?= 10)
GET     /books/pageNumber/:pageNumber                       controllers.BookController.getBooksByPageNumber(pageNumber:Integer)
GET     /books/pageNumber/:minPages/:maxPages               controllers.BookController.getBooksByPageRange(minPages:Integer, maxPages:Integer, size: Integer ?= 10)
GET     /books/price/:price                                 controllers.BookController.getBooksByPrice(price:Integer)
GET     /books/price/:minPrice/:maxPrice                    controllers.BookController.getBooksByPriceRange(minPrice:Integer, maxPrice:Integer, size: Integer ?= 10)
# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.versioned(path="/public", file: Asset)
